server:
  port: 3000

spring:
  data:
    mongodb:
        host: localhost
        port: 27017
        database: rpg-login
        username: root
        password: example
        authentication-database: admin